import { test, expect } from "vitest";
import { Materialite } from "../materialite";

test("Materialite#setSource", () => {
  const materialite = new Materialite();
  const set = materialite.newSet();
});
