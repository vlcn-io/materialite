import { test } from "vitest";

// stress test join and reduce operations
// their implementation seem... naive at best.

// test reduce when all `1`s are sent... so same item is added many times.

test("", () => {});
